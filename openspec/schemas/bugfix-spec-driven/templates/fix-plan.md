# 修复方案

## 一. 问题摘要

**问题描述**：[一句话描述问题]

**JIRA/来源**：[链接]

**严重程度**：[严重/中等/轻微]

---

## 二. 问题现象与复现

### 2.1 现象
- 用户看到的现象：[具体表现]
- 预期行为 vs 实际行为：[对比]

### 2.2 复现条件（关键）
- 数据条件：[什么样的数据会触发]
- 环境条件：[什么环境下会触发]
- 并发条件：[是否涉及并发]

---

## 三. 根本原因【强制】

> 必须找到问题代码，给出具体代码位置和代码片段

### 3.1 调用链追踪（关键）

**入口** → **Controller** → **Service** → **Dao** → **问题点**

具体位置：`ClassName.java:method_name:line`

### 3.2 问题代码【强制】

**代码位置**：`ClassName.java:method_name:start-end`

```java
// 现状代码（有问题的）
public void buggyMethod() {
    // 问题代码
    // ← 这里有问题：[具体问题]
}
```

### 3.3 根本原因

**直接原因**：[代码层面的问题]

**根本原因**：[为什么会这样]

---

## 四. 修复方案

### 4.1 修复思路

**修复策略**：[采用什么策略]

**影响范围**：[会影响哪些地方]

### 4.2 修复代码

**修复前**：
```java
// 现状代码
```

**修复后**：
```java
// 修复后的代码
// 修复点1：[说明]
// 修复点2：[说明]
```

### 4.3 其他需要改的地方（如有）

**位置1**：`ClassName.java:method_name:start-end`
```java
// 需要修改的代码
```

---

## 五. 代码改造分析

> 如果修复涉及多个改造点或关键分支，填写此部分；否则可跳过

### 5.1 入口链路

**代码位置**：`ClassName.java:method_name:start-end`

**现状逻辑**：[描述]

**风险点**：[具体问题]

**改造要点**：
```java
// 改造后的代码
```

### 5.2 核心校验/分支

**代码位置**：`ClassName.java:method_name:start-end`

**现状逻辑**：[描述]

**风险点**：[具体问题]

**改造要点**：
```java
// 改造后的代码
```

### 5.3 数据落点/后置处理

**代码位置**：`ClassName.java:method_name:start-end`

**现状逻辑**：[描述]

**风险点**：[具体问题]

**改造要点**：
```java
// 改造后的代码
```

---

## 六. 输入/输出契约（如有接口变更）

> 如果修复涉及接口变更，填写此部分；否则写"无接口变更"

### 6.1 接口变更

**接口1：POST /api/path/to/endpoint**
- 变更类型：新增 / 修改
- 新增参数：`param1`, `param2`
- 新增返回字段：`field1`, `field2`
- 说明：[接口功能说明]

### 6.2 参数约束

- `param1`：类型/必填/默认值/说明
- `param2`：类型/必填/默认值/说明

### 6.3 返回字段约束

- `field1`：类型/必填/默认值/说明
- `field2`：类型/必填/默认值/说明

### 6.4 错误码

- `ERR_001`：错误描述
- `ERR_002`：错误描述

---

## 七. 边界与异常分支

### 7.1 关键边界

#### 边界条件1：<条件描述>
- 触发条件：<具体场景>
- 处理策略：
```java
if (condition) {
    throw new ServiceException("ERR_CODE", "提示语");
}
```

#### 边界条件2：<条件描述>
- 触发条件：<具体场景>
- 处理策略：<处理方式>

### 7.2 异常链路

#### 异常来源1：<异常类型>
- 触发场景：<具体场景>
- 处理策略：
```java
try {
    // 业务逻辑
} catch (ServiceException e) {
    logger.error("异常日志", e);
    // 重试/补偿/告警
}
```

#### 异常来源2：<异常类型>
- 触发场景：<具体场景>
- 处理策略：<重试/补偿/告警/兜底>

---

## 八. 影响范围

### 8.1 模块影响
1. [模块/服务]：[改动点]

### 8.2 接口影响
1. 新增：[接口描述]
2. 修改：[接口描述]
3. 无接口变更可写"无"

### 8.3 数据影响
1. 是否需要数据修复：[是/否]
2. 修复脚本：[如需要，给出SQL]
3. 无数据变更可写"无"

### 8.4 兼容性影响
- 是否影响现有功能：[是/否]
- 是否需要灰度发布：[是/否]

### 8.5 回归范围
- 需要回归的功能：[列表]
- 需要回归的场景：[列表]

### 8.6 并发/性能影响
- 是否需要加锁：[是/否]
- 是否需要缓存更新：[是/否]

---

## 九. 开发补充要求（可选）
1. [补充说明]
